# Postman MCP Server Setup (local)

This folder contains the setup artifacts created by BLACKBOXAI.

- `blackbox_mcp_settings.json` (configured with server name `github.com/postmanlabs/postman-mcp-server`)
- `mcp_demo.md` (how to demonstrate a tool capability)
